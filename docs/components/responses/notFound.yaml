description: Resource not found
content:
  application/json:
    schema:
      $ref: ../schemas/ValidationError.yaml
    examples:
      NotFound:
        value:
          error: 'Resource not found'
